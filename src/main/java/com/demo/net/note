1.GET和POST的区别?
get请求可以被缓存,保留在历史数据中可以被收藏为书签，post不能被缓存，不能被收藏为书签
get请求有长度限制，post对数据长度没有要求

2.DNS使用的协议?
即使用UDP又使用TCP
TCP:是一种可靠的链接，保证了数据的准确性
UDP:用户数据包协议，不能保证数据的可靠性，顺序性

3.COOKIES和SESSION的区别？
COOKIES是一种能够让网站服务器将少量的数据存放在客户端的内存或者硬盘上面，或者从客户端硬盘上读取数据的一种技术。
SESSION：当用户请求来自web页面时，服务器判断如果用户没有回话，将创建一个session对象，当session过期或者弃用后，回话将终止。

cookies时客户端保存状态的机制，session时服务端保存状态的机制。
session在服务端保存回话状态需要在客户端也保存一个Session ID,唯一标识一个session对象，当服务端创建session对象时，
给客户端相应的包中包含set-cookie字段，里面包含了sid表示session ID
客户端每次发送请求都会带着者session ID

4.三次握手


5.一次完整的HTTP请求过程
域名解析-TCP三次握手-建立TCP连接后发送http请求-服务器响应http请求返回html代码-浏览器解析html代码并请求html代码中的资源（js,css等）
-浏览器对页面进行渲染。

6.TCP如何保证可靠传输？



7.详细介绍HTTP
简单快速：客户端请求服务端时只需要发送请求方法(POST,GET等)和请求路径
无连接:指每次连接只处理一个请求，服务器处理完客户端的请求后并受到客户端的应答后，就断开连接。
无状态:指对于事物处理没有记忆能力，缺少状态意味着，如果后续请求需要前面请求的数据，则必须重传


8.URI和URL的区别
URI:uniform resource identifier统一资源标识符，用来唯一标识一个资源
URL:unifore resource locator统一资源定位符，是一种具体的URI,不仅可以标识一个资源还可以定位这个资源


9.HTTPS和HTTP的区别
https需要到CA申请证书，一般免费证书很少，需要收费
http是超文本传输协议，数据是明文传输；https是具有安全性的ssl加密的传输协议。
http和https使用不同的连接方式，默认端口也不一样，前者80,后者443
http无状态；https是由ssl+http构建的可以进行加密传输和身份认证的网络协议，更加安全。


10.网络编程时的同步、异步、阻塞、非阻塞？
同步和异步关注的是任务完成消息通知的机制，是调用方主动询问是否完成标识是同步，而如果是被调用放主动告知调用方完成任务表示异步。
阻塞和非阻塞等待任务完成时请求者的状态，请求者被挂起表示阻塞，而可以立即抽离去完成其他任务的是非阻塞型的


11.Java如何实现无阻塞方式的Socket编程？
NIO有效解决了多线程服务器存在的线程开销问题。

12.什么是java 的序列化(串行化)？
保存在内存中对象的状态(实例变量不是方法)

13.什么情况下需要序列化？序列化的注意事项，如何实现java 序列化(串行化)？
//1.当想要把对象保存到文件或者是数据库中时
//2.当想要在网络中传送对象时
//3.当想通过RMI(远程方法调用)传送对象时
    序列化注意事项
    1、如果子类实现Serializable接口而父类未实现时，父类不会被序列化，但此时父类必须有个无参构造方法，否则会抛InvalidClassException异常。
    2、静态变量不会被序列化，那是类的“菜”，不是对象的。串行化保存的是对象的状态，即非静态的属性，即实例变量。不能保存类变量。
    3、transient关键字修饰变量可以限制序列化。对于不需要或不应该保存的属性，应加上transient修饰符。要串行化的对象的类必须是公开的（public）。
    4、虚拟机是否允许反序列化，不仅取决于类路径和功能代码是否一致，一个非常重要的一点是两个类的序列化 ID是否一致，
    就是 private static final long serialVersionUID = 1L。
    5、Java 序列化机制为了节省磁盘空间，具有特定的存储规则，当写入文件的为同一对象时，并不会再将对象的内容进行存储，
    而只是再次存储一份引用。反序列化时，恢复引用关系。

    6、序列化到同一个文件时，如第二次修改了相同对象属性值再次保存时候，虚拟机根据引用关系知道已经有一个相同对象已经写入文件，
    因此只保存第二次写的引用，所以读取时，都是第一次保存的对象。

14.java中有几种类型的流？JDK为每种类型的流提供了一些抽象类以供继承，请说出他们分别是哪些类？
  InputStream OutputStream Reader Writer
  按照流向分类:输入流，输出流
  按数据传输单位分：字节流（8bit）字符流(16bit)
  按照功能:节点流和过滤流

15..TCP/IP在连接时有几次握手？释放时有几次握手？
建立连接三次握手
释放连接四次握手


16.单工、半双工和全双工的定义
   如果在通信过程的任意时刻，信息只能由一方A传到另一方B，则称为单工。
   如果在任意时刻，信息既可由A传到B，又能由B传A，但只能由一个方向上的传输存在，称为半双工传输。
   如果在任意时刻，线路上存在A到B和B到A的双向信号传输，则称为全双工。
   scoket是全双工的




